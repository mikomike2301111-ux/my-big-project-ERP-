--- api/rpc.js
+++ api/rpc.js
@@ -638,7 +638,16 @@
     });
 
     // ── Footer split: bank block (left) + totals (right) ──
-    if (y + 120 > pageBottom()) y = addSummaryPage(); // only when footer truly cannot fit
+    // Prefer a single page: only spill summary when many line items force it
+    const needSummaryRoom = 100;
+    if (y + needSummaryRoom > pageBottom()) {
+      if (rows.length > 20) {
+        y = addSummaryPage();
+      } else {
+        // Compress onto page 1 — never emit an almost-empty second page
+        y = Math.max(pageBottom() - needSummaryRoom - 8, y - 24);
+      }
+    }
     y += 10;
     doc.moveTo(left, y).lineTo(right, y).strokeColor('#eee').lineWidth(1).stroke();
     const bankTop = y + 10;
@@ -691,7 +700,7 @@
     // ── KRA + disclaimer footer ──
     doc.moveTo(left, doc.page.height - 42).lineTo(right, doc.page.height - 42).strokeColor('#eee').lineWidth(1).stroke();
     doc.fillColor('#555').fontSize(8).font('Helvetica-Bold').text(`KRA PIN: ${company.pin}`, left, doc.page.height - 36, { width, align: 'center' });
-    doc.fillColor('#888').fontSize(7.5).font('Helvetica-Oblique').text(`Generated by Unity ERP  |  Page ${pageNo}`, left, doc.page.height - 26, { width, align: 'center' });
+    doc.fillColor('#888').fontSize(7).font('Helvetica-Oblique').text(`Generated by FarmTrack ERP  |  Page 1 of 1`, left, doc.page.height - 22, { width, align: 'center' });
     doc.end();
   });
 }
@@ -7164,6 +7173,34 @@
     log(u, 'Generate Tax Invoice PDF', 'Accounts', invNo);
     return { fileName: `tax-invoice-${slug(invoice.customerName || customer.name)}-${slug(invNo)}.pdf`, contentType: 'application/pdf', base64: buffer.toString('base64') };
   },
+  async generatePurchaseOrderPdf(user, poId) {
+    const u = reqRole(user, ROLES.ADMIN, ROLES.MANAGER, ROLES.PROCUREMENT, ROLES.EXECUTIVE, ROLES.DEV);
+    const d = data();
+    const po = (d.purchaseOrders || []).find(p => p.id === poId || p.poNo === poId);
+    if (!po) throw new Error('Purchase order not found');
+    const items = (d.purchaseOrderItems || []).filter(i => i.poId === po.id);
+    const supplier = (d.suppliers || []).find(s => s.id === po.supplierId || s.name === po.supplierName) || {};
+    const { purchaseOrderPdfBuffer } = require('../server/purchaseOrderPdf');
+    const buffer = await purchaseOrderPdfBuffer({
+      po,
+      items,
+      supplier,
+      settings: d.settings || {},
+      shipTo: {
+        name: po.shipToName,
+        company: po.shipToCompany,
+        address: po.warehouseName || po.shipToAddress,
+        city: po.shipToCity,
+        phone: po.shipToPhone,
+      },
+    });
+    log(u, 'Generate Purchase Order PDF', 'Procurement', po.poNo || po.id);
+    return {
+      fileName: `purchase-order-${String(po.poNo || po.id).replace(/[^a-z0-9]+/gi, '-')}.pdf`,
+      contentType: 'application/pdf',
+      base64: buffer.toString('base64'),
+    };
+  },
   async emailTaxInvoice(user, invoiceId, { to: overrideTo, vatMode = 'auto' } = {}) {
